// Warning, this file is autogenerated, please don't change it manually.
// Your changes will get overwritten.

import {Request} from "express";
import { MyContext } from "./types";
import { schema } from "./graphql/schema";

{{#each modules}}import { get{{name}}Context } from "./modules/{{name}}/get{{name}}Context";{{/each}}

const context = ({ req }: { req: Request }): MyContext => {
const cacheKey = req.cookies?.user ?? undefined;
return {
{{#each modules}}...get{{name}}Context(cacheKey),{{/each}}
}
};
export const startupConfig = { schema, context };
