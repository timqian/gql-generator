{{#each queries}}import { {{name}} } from "./Queries/{{name}}";{{/each}}
{{#each mutations}}import { {{name}} } from "./Mutations/{{name}}";{{/each}}
{{#if types}}import { typeResolvers } from "./Types/typeResolvers";{{/if}}

// Warning, this file is autogenerated, please don't change it manually.
// Your changes will get overwritten.

export const {{moduleName}}Resolvers = {
{{#if types}}...typeResolvers,{{/if}}
{{#if queries.length}}Query: { {{#each queries}}...{{name}},{{/each}} },{{/if}}
{{#if mutations.length}}Mutation: { {{#each mutations}}...{{name}},{{/each}} },{{/if}}
};

//   const context = { types: false, queries: [], mutations: [], sectionName };
