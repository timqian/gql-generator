{{#each queries}}import { {{name}}Query } from "./queries/{{name}}Query";{{/each}}
{{#each mutations}}import { {{name}}Mutation } from "./mutations/{{name}}Mutation";{{/each}}
{{#if types}}import { typeResolvers } from "./types/typeResolvers";{{/if}}

// Warning, this file is autogenerated, please don't change it manually.
// Your changes will get overwritten.

export const {{moduleName}}Resolvers = {
{{#if types}}...typeResolvers,{{/if}}
{{#if queries.length}}Query: { {{#each queries}}...{{name}}Query,{{/each}} },{{/if}}
{{#if mutations.length}}Mutation: { {{#each mutations}}...{{name}}Mutation,{{/each}} },{{/if}}
};
